# Agent 问数回归测试清单

数据范围：2016-09 至 2018-09。

## 已完成

- [x] 2017 年信用卡支付的销售额
  - 验证：GMV、年份过滤、支付方式维度值、跨事实表 EXISTS。
  - 结果：4896855.88。
  - 状态：SQL 正确，数据库独立复核一致。

- [x] 2017 年 1 月信用卡支付的销售额
  - 验证：月份精确过滤、支付方式维度值、跨事实表 EXISTS。
  - 结果：98610.25。
  - 状态：SQL 正确，数据库独立复核一致。

- [x] 2017 年的订单量
  - 验证：order_count 指标和下单日期关系。
  - 结果：45101。
  - 状态：SQL 正确，数据库独立复核一致。

- [x] 2017 年按支付方式的支付金额
  - 验证：payment_amount 指标、支付方式分组和日期过滤。
  - 结果：boleto 1396063.37；credit_card 5637373.94；voucher 172982.95；debit_card 43326.47。
  - 状态：SQL 正确，返回 4 个支付方式分组。

- [x] 2017 年每个月的销售额
  - 验证：GMV、按月分组和年度范围过滤。
  - 结果：返回 2017-01 至 2017-12 共 12 行。
  - 状态：SQL 正确。

- [x] 2017 年信用卡支付的订单量
  - 验证：order_count、支付方式过滤、跨事实表 EXISTS。
  - 结果：34415。
  - 状态：SQL 正确，数据库独立复核一致。

- [x] 2017 年 bitcoin 支付的销售额
  - 验证：不存在的维度值不能被错误替换为相似支付方式。
  - 结果：NULL。
  - 状态：SQL 保留用户原始值 bitcoin，没有误映射为 credit_card 等现有值。

- [x] 2023 年信用卡支付的销售额
  - 验证：超出数据时间范围时仍能生成正确 SQL，并返回空结果。
  - 结果：NULL。
  - 状态：SQL 正确；空结果符合当前数据范围。

## 后续测试

- [ ] 2017 年每个月的订单量
- [x] 2017 年每个月的订单量：返回 2017-01 至 2017-12 共 12 行。
- [x] 2017 年平均客单价：138.087597。修复后使用 purchase_date_key 关联，并通过 year_month_value 过滤。
- [x] 2017 年平均配送天数：13.005486。
- [x] 2017 年延迟率：修复后按 purchase_date_key 统计，结果 0.0638。
- [x] 2017 年平均评价分数：修复后默认按 review_creation_date_key 统计，结果 4.1095。
- [x] 2017 年好评率：修复后默认按 review_creation_date_key 统计，结果 0.7769。
- [x] 2017 年按订单状态统计订单量：返回 8 个状态分组。
- [x] 2017 年按商品品类统计销售额：返回 72 个品类分组；DW 清洗后展示值不再包含 `\r`。
- [x] 2017 年按卖家州统计销售额：返回 20 个州分组。
- [x] 2017 年按客户州统计订单量：返回 22 个州分组。

## 本轮发现的问题

- [x] 多日期字段默认选择不稳定：已固定订单类默认 `purchase_date_key`、评价类默认 `review_creation_date_key`；用户明确指定时仍可切换其他日期关系。
- [x] 日期过滤表达式不统一：已在 SQL 上下文和 Prompt 中禁止 `date_key LIKE`，年份和月份统一使用 `dim_date.year_month_value`。
- [x] 部分商品品类展示值包含 `\r`：已清洗 `dw.dim_category` 和 `dw.dim_product`，并修正 DW 初始化脚本。

## 判断规则

- 指标必须使用 meta.metrics.expression_sql。
- 年份默认按下单日期过滤，除非问句明确指定审批、收货、预计送达等日期。
- relationship_type=filter_exists 时必须使用 EXISTS，避免一对多 JOIN 重复聚合。
- 精确召回的维度值使用 raw_value。
- 不存在的维度值不能自动替换成相似的真实值。
- 无数据时允许聚合结果为 NULL；这是数据为空，不代表 SQL 执行失败。
